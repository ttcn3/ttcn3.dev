# Sources for ttcn3.dev

This is the source repository for https://ttcn3.dev/

## Development

Install dependencies:

	npm install

Run server:

	npm run start

* install Hugo
* install firebase
      npm install -g firebase-tools
